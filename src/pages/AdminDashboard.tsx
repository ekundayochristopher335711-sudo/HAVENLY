import { ShieldCheck, Home, Users, MessageCircle, CheckCircle2 } from 'lucide-react'
import { Container, Badge } from '../components/ui'
import { demoProperties } from '../data/demo'

export function AdminDashboard() {
  const stats = [['1,284', 'Total users', Users], ['326', 'Published homes', Home], ['74', 'New inquiries', MessageCircle], ['19', 'Pending review', ShieldCheck]]
  return <main className="pt-20"><Container><div className="border-b border-black/10 py-10"><p className="text-xs font-bold uppercase tracking-[.18em] text-moss">Administration</p><h1 className="mt-3 font-display text-6xl">Platform overview.</h1></div><div className="grid gap-4 py-8 sm:grid-cols-2 lg:grid-cols-4">{stats.map(([value, label, Icon]) => { const I = Icon as typeof Home; return <div key={label as string} className="rounded-[24px] bg-white p-6 shadow-soft"><I size={18} className="text-moss" /><p className="mt-8 text-3xl font-bold">{value as string}</p><p className="mt-1 text-sm text-black/45">{label as string}</p></div> })}</div><section className="py-8"><div className="flex items-center justify-between"><h2 className="text-xl font-semibold">Listings needing attention</h2><Badge>Live data</Badge></div><div className="mt-5 overflow-hidden rounded-[24px] border border-black/10 bg-white">{demoProperties.slice(0, 5).map((p) => <div key={p.id} className="grid items-center gap-4 border-b border-black/5 p-4 last:border-0 sm:grid-cols-[64px_1fr_auto]"><img src={p.image} className="size-16 rounded-xl object-cover" alt="" /><div><p className="font-semibold">{p.title}</p><p className="text-xs text-black/40">{p.city} · {p.property_type}</p></div><span className="flex items-center gap-1 text-xs font-semibold text-moss"><CheckCircle2 size={14} /> Verified</span></div>)}</div></section></Container></main>
}
